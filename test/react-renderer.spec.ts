import { ReactRenderer } from '../lib/react-renderer'
import { faker } from '@faker-js/faker'
import { Actor } from 'tarant'

describe('ReactRenderer', () => {
  it('should bind with react in initialize', () => {})
})
